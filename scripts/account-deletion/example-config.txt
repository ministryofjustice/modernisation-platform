# example-config.txt

# Define an array of workspaces to manage
WORKSPACES=("development" "test" "preproduction" "production") 

# Define the Application name
APPLICATION_NAME="example"

# Is this operation for a member account? (yes/no)
MEMBER_ACCOUNT="yes"

# Are you deleting ALL of the remaining environments for the application? (yes/no)
COMPLETE_DELETION="no"

# Define the Modernisation Platform Account ID
MODERNISATION_ACCOUNT_ID=""

# Define the absolute path to the Modernisation Platform repo on your device
USER_MP_DIR="/Users/kudzai.mtoko/mojgithub/modernisation-platform"

# Define the absolute path to the Modernisation Platform Environments repo on your device
USER_MPE_DIR="/Users/richard.green/Documents/GitHub/modernisation-platform-environments"

# Define the Temporary Administrator Access Modernisation Platform Account Credentials
MP_CREDENTIALS=(
    
)

# Define the Temporary Administrator Access Development Account Credentials
DEVELOPMENT_CREDENTIALS=""

# Define the Temporary Administrator Access Test Account Credentials
TEST_CREDENTIALS=""

# Define the Temporary Administrator Access Preproduction Account Credentials
PREPRODUCTION_CREDENTIALS=""

# Define the Temporary Administrator Access Production Account Credentials
PRODUCTION_CREDENTIALS=""

