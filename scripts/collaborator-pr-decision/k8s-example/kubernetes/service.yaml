apiVersion: v1
kind: Service
metadata:
  name: collaborator-pr-decision
  namespace: your-namespace
spec:
  selector:
    app: collaborator-pr-decision
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
